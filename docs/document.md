方針は承認します。この内容で実装を進めてください。

ただし、以下を追加・明確化してください。

1. おやすみ時間による調整後の時刻は、
   nowより後、かつdeadlineより前の場合だけ採用する

2. おやすみ時間帯を[start, end)として定義し、
   繰り上げ先は「おやすみ開始時刻の1分前」とする
   「直前」のような曖昧な定義にはしない

3. setQuietHoursはローカル更新だけでなく、
   既存setterと同じサーバー同期・serverVersion CAS・
   エラー処理を通す
   時刻は常にHH:mm形式へ正規化する

4. 日時の計算は端末のローカルタイムゾーンを基準とし、
   日またぎ、日付変更、DSTがある地域でもDate計算で処理する
   固定の時差加算では実装しない

5. 今回は既存画面のuseEffectで再スケジュールするが、
   アプリ起動時やbootstrap後の再構築は将来課題として
   ドキュメントへ明記する

6. 時間帯UIは1時間刻みでよい
   24個の選択肢は横スクロールまたは折り返し表示にし、
   開始・終了のどちらを選択中か明示する

追加テスト:
- 繰り下げ先がすでに過去なら採用しない
- 調整先がdeadlineと同時刻ならスキップ
- 22:00〜07:00で22:00、06:59、07:00の境界
- setQuietHoursのHH:mm正規化
- サーバー同期失敗時の状態
- pushEnabled OFF時に既存3通知すべてがキャンセルされる

以上を反映して実装してください。